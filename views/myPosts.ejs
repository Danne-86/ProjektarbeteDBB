<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/stylesheets/style.css" />
</head>
<body class="bg-gray-50 min-h-screen">
  <div class="container mx-auto px-4 py-8 max-w-3xl">
    <div class="flex items-center justify-between mb-6">
      <h1 class="text-3xl font-bold">My posts</h1>
      <a href="/blog/new" class="px-4 py-2 rounded bg-indigo-600 text-white hover:bg-indigo-700">New post</a>
    </div>

    <% if (!posts || posts.length === 0) { %>
      <p class="text-gray-600">You haven’t published anything yet.</p>
    <% } else { %>
      <ul class="space-y-4">
        <% posts.forEach(p => { %>
          <li class="bg-white rounded-xl shadow p-5">
            <div class="flex items-start justify-between gap-4">
              <div class="flex-1">
                <a href="/feed/<%= p.id %>" class="block hover:underline">
                  <h2 class="text-xl font-semibold mb-2"><%= p.header %></h2>
                </a>
                <% if (p.hero_image) { %>
                  <img src="<%= p.hero_image %>" alt="Post image" class="w-full rounded mb-3 max-h-60 object-cover" />
                <% } %>
                <p class="text-gray-700 mb-3"><%= p.excerpt %></p>
                <div class="text-sm text-gray-500">
                  Published · <%= new Date(p.created_at).toLocaleString() %>
                </div>
              </div>
            </div>
          </li>
        <% }) %>
      </ul>
    <% } %>
  </div>
</body>
</html>