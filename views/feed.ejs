<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 min-h-screen">
  <div class="container mx-auto px-4 py-8 max-w-3xl">

    <h1 class="text-3xl font-bold mb-6">Latest posts</h1>

    <% if (!posts || posts.length === 0) { %>
      <p class="text-gray-600">No posts yet.</p>
    <% } else { %>
      <ul class="space-y-4">
        <% posts.forEach(p => { %>
          <li class="bg-white rounded-xl shadow p-5">
            <a href="/posts/<%= p.id %>" class="block">
              <h2 class="text-xl font-semibold mb-2 hover:underline"><%= p.header %></h2>
            </a>

            <% if (p.hero_image) { %>
              <img src="<%= p.hero_image %>" alt="Post image" class="w-full rounded mb-3 max-h-60 object-cover" />
            <% } %>

            <p class="text-gray-700 mb-3"><%= p.excerpt %></p>
            <div class="text-sm text-gray-500">
              By <span class="font-medium"><%= p.username %></span>
              · <%= new Date(p.created_at).toLocaleString() %>
            </div>
          </li>
        <% }) %>
      </ul>
    <% } %>
  </div>
</body>
</html>